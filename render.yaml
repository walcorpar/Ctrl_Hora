services:
  - type: web
    name: ctrl-hora
    env: python
    plan: free
    buildCommand: |
      sudo apt-get update
      sudo apt-get install -y libdbus-1-dev
      pip install -r requirements.txt
    startCommand: gunicorn Ctrl_Hora:app
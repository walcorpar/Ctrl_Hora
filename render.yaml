services:
  - type: web
    name: ctrl-hora
    env: python
    plan: free
    buildCommand: |
      apt-get update
      apt-get install -y libdbus-1-dev
      pip install -r requirements.txt
    startCommand: gunicorn Ctrl-Hora:app